rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }

    // Primitive definition
    function incoming() { return request.resource.data; }
    function existing() { return resource.data; }
    function isSignedIn() { return request.auth != null; }
    function isValidId(id) { 
        return id is string && id.size() > 0 && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$'); 
    }
    
    function isAdmin(uid) {
      return exists(/databases/$(database)/documents/users/$(uid)/profile/info) &&
             get(/databases/$(database)/documents/users/$(uid)/profile/info).data.role == 'admin';
    }

    // Validation Blueprint for Task
    function isValidTask(data, uid) {
      return data.keys().hasAll(['userId', 'title', 'priority', 'location', 'status', 'createdAt', 'updatedAt']) 
             && data.keys().size() >= 7
             && data.userId == uid
             && data.title is string && data.title.size() <= 200
             && data.priority is string && data.priority.matches('^(low|medium|high)$')
             && data.location is string && data.location.size() <= 200
             && data.status is string && data.status.matches('^(pending|in-progress|completed)$')
             && data.createdAt is timestamp
             && data.updatedAt is timestamp;
    }

    // Validation Blueprint for Activity
    function isValidActivity(data, uid) {
      return data.keys().hasAll(['userId', 'type', 'area', 'status', 'date', 'createdAt', 'updatedAt'])
             && data.keys().size() >= 7
             && data.userId == uid
             && data.type is string && data.type.size() <= 100
             && data.area is string && data.area.size() <= 100
             && data.status is string && data.status.size() <= 100
             && data.createdAt is timestamp
             && data.updatedAt is timestamp;
    }

    // Validation Blueprint for Inventory
    function isValidInventory(data, uid) {
      return data.keys().hasAll(['name', 'category', 'currentStock', 'unit', 'minThreshold', 'lastUpdated', 'createdAt', 'updatedAt'])
             && data.name is string
             && data.category is string
             && data.currentStock is number
             && data.unit is string
             && data.minThreshold is number
             && data.lastUpdated is string
             && data.createdAt is timestamp
             && data.updatedAt is timestamp;
    }

    // Validation Blueprint for Incident
    function isValidIncident(data) {
      return data.keys().hasAll(['type', 'severity', 'facility', 'summary', 'reportedBy', 'status', 'createdAt'])
             && data.type is string
             && data.severity is string
             && data.facility is string
             && data.summary is string
             && data.reportedBy is string
             && data.status is string
             && data.createdAt is timestamp;
    }

    match /users/{userId}/tasks/{taskId} {
      allow read: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
      
      allow create: if isSignedIn() 
                    && (request.auth.uid == userId || isAdmin(request.auth.uid))
                    && isValidId(taskId)
                    && isValidId(userId)
                    && isValidTask(incoming(), request.auth.uid)
                    && incoming().createdAt == request.time
                    && incoming().updatedAt == request.time;
                    
      allow update: if isSignedIn()
                    && (request.auth.uid == userId || isAdmin(request.auth.uid))
                    && existing().userId == request.auth.uid
                    && isValidId(taskId)
                    && isValidId(userId)
                    && isValidTask(incoming(), request.auth.uid)
                    && incoming().createdAt == existing().createdAt
                    && incoming().updatedAt == request.time
                    && (
                      // Action: Standard update
                      incoming().diff(existing()).affectedKeys().hasOnly(['title', 'priority', 'location', 'description', 'assignedTo', 'deadline', 'isSynced', 'status', 'updatedAt', 'photoUrl', 'consumedParts', 'linkedActivity', 'joNumber', 'accountNumber', 'accountName'])
                    );

      allow delete: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid)) && existing().userId == request.auth.uid;
    }

    match /users/{userId}/profile/{docId} {
       allow read: if isSignedIn();
       allow write: if isSignedIn() && request.auth.uid == userId;
    }

    match /users/{userId}/inventory/{itemId} {
      allow read: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
      
      allow create: if isSignedIn() 
                    && (request.auth.uid == userId || isAdmin(request.auth.uid))
                    && isValidId(itemId)
                    && isValidId(userId)
                    && isValidInventory(incoming(), request.auth.uid)
                    && incoming().createdAt == request.time
                    && incoming().updatedAt == request.time;
                    
      allow update: if isSignedIn()
                    && (request.auth.uid == userId || isAdmin(request.auth.uid))
                    && isValidId(itemId)
                    && isValidId(userId)
                    && isValidInventory(incoming(), request.auth.uid)
                    && incoming().createdAt == existing().createdAt
                    && incoming().updatedAt == request.time;

      allow delete: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }

    match /{path=**}/tripTickets/{ticketId} {
      allow read: if isSignedIn() && isAdmin(request.auth.uid);
    }
    
    match /{path=**}/attendance/{attendanceId} {
      allow read: if isSignedIn() && isAdmin(request.auth.uid);
    }

    match /shared/{docId} {
      allow read, write: if isSignedIn() && isAdmin(request.auth.uid);
    }

    match /users/{userId}/tripTickets/{ticketId} {
      allow read: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
      allow write: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }

    match /users/{userId}/activities/{activityId} {
      allow read: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
      
      allow create: if isSignedIn() 
                    && (request.auth.uid == userId || isAdmin(request.auth.uid))
                    && isValidId(activityId)
                    && isValidId(userId)
                    && isValidActivity(incoming(), request.auth.uid)
                    && incoming().createdAt == request.time
                    && incoming().updatedAt == request.time;
                    
      allow update: if isSignedIn()
                    && (request.auth.uid == userId || isAdmin(request.auth.uid))
                    && existing().userId == request.auth.uid
                    && isValidId(activityId)
                    && isValidId(userId)
                    && isValidActivity(incoming(), request.auth.uid)
                    && incoming().createdAt == existing().createdAt
                    && incoming().updatedAt == request.time
                    && (
                      // Action: Standard update
                      incoming().diff(existing()).affectedKeys().hasOnly(['type', 'area', 'siteOrWell', 'blockLot', 'staff', 'date', 'status', 'isSynced', 'details', 'updatedAt'])
                    );

      allow delete: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid)) && existing().userId == request.auth.uid;
    }

    match /users/{userId}/attendance/{attendanceId} {
      allow read, write: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }
    match /users/{userId}/areas/{areaId} {
      allow read, write: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }
    match /users/{userId}/staff/{staffId} {
      allow read, write: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }
    match /users/{userId}/activityTypes/{typeId} {
      allow read, write: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }
    match /users/{userId}/incidents/{incidentId} {
      allow read: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
      allow create: if isSignedIn() 
                    && (request.auth.uid == userId || isAdmin(request.auth.uid))
                    && isValidId(incidentId)
                    && isValidIncident(incoming());
      allow update: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid)) && isValidIncident(incoming());
      allow delete: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }
    match /users/{userId}/pmsSchedules/{scheduleId} {
      allow read, write: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }
    match /users/{userId}/handovers/{handoverId} {
      allow read, write: if isSignedIn() && (request.auth.uid == userId || isAdmin(request.auth.uid));
    }
  }
}
